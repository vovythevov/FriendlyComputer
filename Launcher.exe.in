@ECHO off

:: Only set the path if it wasn't set before
IF NOT DEFINED LostComputer_PATH GOTO :set_path
GOTO :run_command

:set_path
ECHO Adding LostComputer dpendencies to the PATH
SET LostComputer_PATH_PATH=@launcher_library_dirs@;
SET PATH=%LostComputer_PATH_PATH%;%PATH%

:run_command
%FriendlyComputer_EXECUTABLE%
IF %ERRORLEVEL% NEQ 0 (
  EXIT 1
) ELSE (
  EXIT 0
)

:: Exit and pass along our exit code.
